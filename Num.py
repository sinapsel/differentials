#-*-coding:utf8;-*-

class Numer(float):
    '''def __add__(self, other):
        return self + other
    def __neg__(self):
        return -self
    def __mul__(self, a):
        return self * a
    def __sub__(self, a):   # a - b = a + (-b)
        return self + (-a)
    def __radd__(self, a):      # b + a = a + b
        return self + a
    def __rsub__(self, a):      # b - a = a + (-b)
        return a + (-self)
    def __rmul__(self, a):      # b * a = a * b
        return a * self    
    def __iadd__(self, a):      # a += b
        self = self + a
        return self
    def __isub__(self, a):      # a -= b
        self = self - a
        return self
    def __imul__(self, a):      # a *= b
        self = self * a
        return self
    '''
    pass
